import axios, { Axios } from "axios";

import Header from "../Layout/Header/Header";

const logIn = (isExist: any) => {
  const ifuserCanLogIn = isExist;
  if (ifuserCanLogIn === true) {
  }
};
export default logIn;
